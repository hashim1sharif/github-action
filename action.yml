name: "Hello Action"
description: "Greets someone"

inputs:
  who-to-greet:
    description: "who to greet"
    required: true
    default: "world"

    outputs:
    time:
      description: "time we greeted"
runs:
  using: "node20"
  main: "dist/index.js"
